[Unit]
# A cold TCG boot can exhaust UDisks' startup budget while activating Polkit.
# Start its required authorization manager before starting the disk manager.
Requires=polkit.service
After=polkit.service
